---
prompt_id: "PROMPT(TAURWORKS_INIT_CREATE_DOGFOOD_POLISH)[POLISH/2026-05-13]"
work_item: "AD_HOC"
slug: "taurworks-init-create-dogfood-polish"
status: "landed"
date: "2026-05-14"
---

# Summary
Polished the init/create/resolver dogfood workflows with README examples, command-help wording, and workflow-focused CLI tests.

# Result
- Added README dogfood examples for creating from a parent directory, initializing an existing directory, changing `working_dir`, target-aware show/activate, accidental nesting protection, and read-only activation guidance.
- Clarified `project create --help` so it describes creating a new project root and directs existing/current roots to `project init`.
- Added tests for help-text distinctions and the practical create/init/working-dir/activate dogfood workflows.

# Validation
- Passed: `./scripts/lint`.
- Passed: `./scripts/test`.
- Passed: `./scripts/smoke`.
- Blocked by environment/network: `python -m pip install -e .` failed while trying to fetch isolated build dependencies because the package index tunnel returned HTTP 403 for setuptools.
- Passed workaround: `python -m pip install --no-build-isolation -e .`.
- Passed: temporary dogfood workflow under `/tmp/taurworks-dogfood-demo` using `taurworks project create TestProject --working-dir test_repo --create-working-dir`, `taurworks project working-dir show TestProject`, `taurworks project activate TestProject --print`, and the inside-project `taurworks project activate TestProject --print` resolver check.
- Blocked by missing helper: `scripts/prompts/record-execution --help` failed because `scripts/prompts/record-execution` is not present in this checkout, so this execution record was created manually using `project/executions/README.md` conventions.

# Follow-up
- Restore or add the documented `scripts/prompts/record-execution` helper if prompt execution records should be generated by script in this repository.
