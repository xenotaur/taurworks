---
execution_id: 2026_07_25_02_04_05_WI_BIN_REPO_SPLIT_0001
prompt_id: PROMPT(WI-BIN-REPO-SPLIT-0001:WI_BIN_REPO_SPLIT_0001)[2026-07-25T00:24:18-04:00]
work_item: WI-BIN-REPO-SPLIT-0001
status: landed
rerun_of:
pr: https://github.com/xenotaur/taurworks/pull/88
commit: 6cd1210ab58e472789f8a95435457d82016800e1
created_at: 2026-07-25T02:04:05-04:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-BIN-REPO-SPLIT-0001.md
session_transcript: claude-app:43689ae3-1b8e-45ff-b3b8-75e8997239fb
---

# Summary

Implemented `WI-BIN-REPO-SPLIT-0001`. This execution session involved a
significant mid-session design correction before implementation began: an
initial audit-driven plan (delete `bin/`'s personal-dotfile files as
unused dead weight) was caught and rejected by the user before any file
changes were made, on the grounds that `bin/`'s scripts are load-bearing
personal daily-use tooling the user relies on more than `taurworks`
itself, and preserving them under source control is a hard invariant. A
follow-up design discussion (grounded in an actual repo-wide PII audit —
367/373 commits already publicly authored under the user's real
name/email, `bin/`'s own content found to have no compromising material
beyond that) settled on: split `bin/` (and its man page) into a new
private sibling repo with full history preserved, leaving `taurworks`'
own existing public history untouched (no rewrite needed, since the PII
concern was resolved as non-issue).

# Result

**Audit (Required Change #1):** Checked all ~26 non-`migrate_legacy_projects.py`
files in `bin/` (plus `man/man1/drq.1`) against `src/`, `tests/`,
`scripts/`, `.github/workflows/`, `setup.py` — zero references anywhere,
confirming they're not consumed by `taurworks` code. Full-history scan
(`git log -p --all -- bin/`) found no credentials/tokens/keys, only
already-acceptable identity info (commit authorship, `dresan.com`) and
generic environment boilerplate (`/home/parallels`, one stale AWS EC2
hostname). **Disposition: split into `xenotaur/taurscripts` (private),
preserving full history** — not deletion, correcting an earlier flawed
in-session conclusion.

**Extraction:** Two-pass `git filter-repo` on a fresh, isolated clone
(never on the working directory): `--path bin/ --path man/man1/drq.1`,
then `--invert-paths --path bin/migrate_legacy_projects.py` (the 3
commits that only ever touched that one file became empty and were
auto-pruned). Result: exactly 19 commits (2017–2025), matching the
19-of-21 genuine personal-script commits identified in the audit. Pushed
to a newly created private repo, `xenotaur/taurscripts`.

**Implementation in `taurworks`** (ordinary forward commit, no history
rewrite):
- `bin/` and `man/` removed entirely.
- `bin/migrate_legacy_projects.py` → `src/taurworks/tools/migrate_legacy_projects.py`
  (new `tools/` subpackage, kept out of `taurworks`'s normal import
  surface — still loaded by file path in its test); updated
  `tests/migrate_legacy_projects_test.py`'s `_MIGRATE_PATH`.
- `sourceme/`'s 3 files → `src/taurworks/resources/sourceme/`; added the
  new path to `setup.py`'s `package_data`. This directly fixes the
  packaging gap PR #84/#85 review caught (`package_dir={"": "src"}`
  meant paths outside `src/taurworks` were silently excluded from built
  wheels).
- Found and fixed an additional stale reference not covered by the WI's
  own `artifacts_expected` list: `tests/aliases_source_test.py` hardcoded
  `sourceme/aliases.source`'s old path, causing 6 test failures on first
  `./scripts/test` run — corrected to the new location.
- Updated `README.md` (the `tl`/checkout-path instructions and the
  "Legacy shell utility inventory" section) and
  `project/context/humans.md` (a live repo-orientation doc that
  incorrectly still described `bin/` as part of the repo).

# Validation

- `git rev-parse HEAD`: `9c26bb2`
- Tool versions (Taurworks conda env): Python 3.11.10, black 26.3.1, ruff 0.15.12
- `./scripts/format --check --diff`: 30 files unchanged, pass
- `./scripts/lint`: black + ruff, pass
- `./scripts/test`: 288 tests, OK (after fixing the `aliases_source_test.py`
  path regression found above)
- `python -m build --wheel` + `unzip -l` inspection: confirmed
  `taurworks/resources/sourceme/{aliases,completions,man}.source` and
  `taurworks/tools/migrate_legacy_projects.py` all present in the built
  wheel — the item-specific validation this WI's `## Validation` section
  required.
- `lrh validate`: 4 errors, all `contributors/contributors.md` — a
  confirmed genuinely pre-existing gap on `master`, unrelated to this PR.
- New `xenotaur/taurscripts` repo verified: `PRIVATE`, 19 commits, `bin/`
  (minus `migrate_legacy_projects.py`) + `man/man1/drq.1`.

CHAIN-NOTE: cycles=1; stops=1; gates=[merge]; friction="missed one test file's hardcoded sourceme/ path in initial audit scope, causing 6 test failures on first run"; note="PR #88 merged 6cd1210; bin/ split into private xenotaur/taurscripts with full history; WI-BIN-REPO-SPLIT-0001 resolved at closeout"

# Follow-up

- `session_transcript: pending` should be updated to
  `claude-app:<session-id>` after this session ends.
- The WI's own acceptance criterion "disposition decision... recorded
  with rationale in this WI's resolution" should be fulfilled at closeout
  time using the audit findings and taurscripts-split rationale recorded
  in this Result section.
- Next steps: wait for reviewer comments and run
  `/lrh-review-response`/`/lrh-confirm-fixes` on PR #88, then
  `/lrh-closeout` after merge.
