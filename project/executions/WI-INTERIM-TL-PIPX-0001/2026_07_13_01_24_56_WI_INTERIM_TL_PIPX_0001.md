---
execution_id: 2026_07_13_01_24_56_WI_INTERIM_TL_PIPX_0001
prompt_id: PROMPT(WI-INTERIM-TL-PIPX-0001:WI_INTERIM_TL_PIPX_0001)[2026-07-12T00:42:06-04:00]
work_item: WI-INTERIM-TL-PIPX-0001
status: landed
rerun_of: 
pr: https://github.com/xenotaur/taurworks/pull/67
commit: 3e62655
created_at: 2026-07-13T01:24:56-04:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-INTERIM-TL-PIPX-0001.md
session_transcript: claude-app:149f0939-369e-4d12-afa5-29a079bb476f
---

# Summary

Implement WI-INTERIM-TL-PIPX-0001: replace the colliding previous-generation
`tw()` in `sourceme/aliases.source` with a feature-frozen `tl()` interim
activation helper, reconcile the README's install guidance to the
local-checkout pipx path (the package is not on PyPI), and record the `tl`
retirement criterion in writing.

# Result

- `sourceme/aliases.source`: `tw()` replaced by `tl()` — `tl activate NAME`
  looks up `NAME/Admin/project-setup.source` then
  `NAME/.taurworks/project-setup.source` under the workspace root and
  sources it; usage error returns 2, missing-script error returns 1. The
  old delegation of non-activate commands to `taurworks` was dropped
  (feature freeze). Header comments document the freeze and the retirement
  criterion.
- `sourceme/completions.source`: the legacy four-command completion is no
  longer registered for `tw` (it misdescribed the supported helper from
  `taurworks shell print`); kept for `taurworks`; none added for `tl`.
- `README.md`: install section now instructs
  `pipx install --editable <path-to-checkout>` with the env-switch
  rationale, plain-pip variant, and `scripts/develop` note; new
  "Interim `tl` helper" section documents temporary status, feature freeze,
  and the retirement criterion (delete `tl` + `aliases.source` once every
  weekly-activated project works via `tw activate`).
- Environment side effect (user-approved at the plan gate): pipx installed
  via `pip install --user pipx`, and taurworks editable-installed from the
  main checkout for real.

Acceptance criteria verified live:
- Both helpers sourced together: `tw` bound to the supported helper
  (contains `_tw_activate`), `tl` bound to the interim helper.
- `tl activate LCATS` in a real bash shell switched `$CONDA_DEFAULT_ENV`
  from `base` to `LCATS`, changed directory to `~/Workspace/LCATS/LCATS`,
  and printed the readiness message.
- `taurworks --help` works via the `~/.local/bin` pipx shim with the LCATS
  Conda environment active (a non-Taurworks env).

# Validation

- Tool versions: Python 3.11.8, black 26.3.1, ruff 0.15.12
  (`scripts/version tools` does not exist in this repo; direct commands
  used as the equivalent)
- `scripts/format --check --diff` — 26 files unchanged
- `scripts/lint` — all checks passed
- `scripts/test` — 193 tests OK
- `lrh validate` — no new errors (4 known pre-existing `contributors.md`
  errors remain)
- WI-specific: both-helpers `type tl && type tw` check; live
  `tl activate LCATS` conda-switch test; pipx shim test in LCATS env

# Follow-up

- Confirm `~/.local/bin` is on the interactive shell PATH (pipx's shim
  location); if not, the user runs `pipx ensurepath` or adds it manually —
  Taurworks policy forbids editing shell startup files automatically.
- On merge: closeout via /lrh-closeout (mark this record landed, resolve
  the work item).
- Next plan step: WI-LEGACY-BATCH-MIGRATION-0001.
